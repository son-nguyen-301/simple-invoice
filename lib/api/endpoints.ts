/** Upstream service path (no leading slash), relative to the API gateway base. */
export const USERS_ME_PATH = "membership-service/1.0.0/users/me";

/** Upstream invoice-service list/create path (no leading slash). */
export const INVOICES_PATH = "invoice-service/1.0.0/invoices";
